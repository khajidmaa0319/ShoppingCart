export class Order {
    id: number;
    name: string;
    address: string;
   
  
    constructor(id, name, address ) {
      this.id = id
      this.name = name
      this.address = address
      
    }
  }